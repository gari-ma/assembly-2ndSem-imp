; code created by npgarima

; A set of six data bytes is stored starting from memory location 2050H. The set includes some
; blank spaces (bytes with zero values). WAP to eliminate the blanks from the block.