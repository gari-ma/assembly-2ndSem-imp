; Write an assembly language program to count no. of –ve element in a data block containing
; 16 bytes of data; store the count at the end of the block if the count is greater than 8 otherwise
; stores 0. 